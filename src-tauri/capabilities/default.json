{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "fs:default",
    "os:default",
    "shell:default",
    "fs:allow-app-write",
    "fs:allow-app-read",
    "fs:allow-home-read",
    "fs:allow-home-write"
  ]
}
